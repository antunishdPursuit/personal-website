"use client";
import { useState } from "react";
const stories = [
  ["SIGNAL 01", "Align the team", "A hackathon team had tension around a stubborn teammate and competing ideas.", "I adapted to their collaboration style, made room for the team’s ideas, and kept everyone focused on one coherent project.", "We earned third place in the competition.", "Good teamwork is not forcing agreement. It is creating enough clarity and trust for people to contribute to one plan."],
  ["SIGNAL 02", "Teach the system, not just the steps", "A student thought they were building RAG, but their project used text splitting and similarity matching only.", "I mapped the flow from Markdown chunks to similarity search, then contrasted it with a true RAG system that includes a language model or API call.", "They understood what their system did and what would be needed to turn it into RAG.", "Clear teaching starts with the system underneath the label."],
  ["SIGNAL 03", "Recover the plan", "One of 12 volunteers withdrew the day before an event.", "I reviewed appropriate prior volunteer records and interviews, then coordinated with the team to secure a qualified replacement.", "The event ran smoothly, and both the organizers and the replacement volunteer had a good experience.", "Good preparation creates options when plans change."],
];
export default function Stories() { const [active, setActive] = useState(0); const s = stories[active]; const move = (n: number) => setActive((i) => (i + n + stories.length) % stories.length); return <section id="stories" className="mx-auto max-w-6xl px-6 py-28 md:px-12 md:py-40"><div className="mb-16 border-b border-white/10 pb-6"><span className="font-mono text-xs uppercase tracking-[0.3em] text-lime">{"// how I work with a team"}</span><h2 className="display mt-4 text-[clamp(2.5rem,7vw,5.5rem)] text-paper">Signals I bring to a team</h2></div><div className="signal-console"><div className="flex justify-between border-b border-white/10 pb-5 font-mono text-xs"><span className="tracking-[.2em] text-cyan">{s[0]}</span><span className="text-muted">{active + 1} / {stories.length}</span></div><h3 className="display mt-8 text-3xl text-paper md:text-5xl">{s[1]}</h3><div className="mt-8 grid gap-6 md:grid-cols-2">{[["Situation",s[2]],["Action",s[3]],["Result",s[4]],["Learning",s[5]]].map(([label, text]) => <p key={label} className="leading-relaxed text-muted"><span className="signal-label">{label}</span>{text}</p>)}</div><div className="mt-10 flex items-center justify-between border-t border-white/10 pt-5"><div className="flex gap-2">{stories.map((item,i)=><span key={item[0]} className={`h-1.5 w-8 rounded-full ${i===active?"bg-lime":"bg-white/15"}`}/>)}</div><div className="flex gap-3"><button onClick={()=>move(-1)} className="signal-button" aria-label="Previous story">←</button><button onClick={()=>move(1)} className="signal-button" aria-label="Next story">→</button></div></div></div></section>; }
