"use client";
import { useEffect, useState } from "react";
import { useLenis } from "lenis/react";
const links = [["Journey", "#journey"], ["Stories", "#stories"], ["Work", "#work"], ["Credentials", "#certificates"], ["Contact", "#contact"]];
export default function Nav() { const [scrolled,setScrolled]=useState(false); const [open,setOpen]=useState(false); const lenis=useLenis(); const go=(target:string)=>{if(lenis) lenis.scrollTo(target); else document.querySelector(target)?.scrollIntoView({behavior:"smooth"});setOpen(false)}; useEffect(()=>{const update=()=>setScrolled(scrollY>40);update();addEventListener("scroll",update,{passive:true});return()=>removeEventListener("scroll",update)},[]); return <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${scrolled?"border-b border-white/10 bg-ink/80 backdrop-blur-xl":""}`}><nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5 md:px-12"><button onClick={()=>go("#top")} className="font-display text-lg font-bold tracking-tight text-paper" aria-label="Back to top">D<span className="text-lime">A</span></button><div className="hidden items-center gap-1 rounded-full border border-white/10 bg-white/[.03] px-1.5 py-1.5 backdrop-blur-sm md:flex">{links.map(([label,href])=><button key={href} onClick={()=>go(href)} className="min-h-11 rounded-full px-4 text-sm text-muted transition-colors hover:bg-white/5 hover:text-lime">{label}</button>)}</div><button onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="site-menu" className="min-h-11 rounded-full border border-white/10 px-4 font-mono text-xs uppercase tracking-wider text-paper hover:border-lime hover:text-lime md:hidden">Menu</button><a href="https://github.com/antunishdPursuit" target="_blank" rel="noopener noreferrer" className="hidden text-sm text-muted hover:text-lime sm:block">GitHub ↗</a></nav>{open?<div id="site-menu" className="mx-6 rounded-2xl border border-white/10 bg-ink/95 p-2 shadow-2xl backdrop-blur-xl md:hidden">{links.map(([label,href])=><button key={href} onClick={()=>go(href)} className="block min-h-11 w-full rounded-xl px-4 text-left text-sm text-paper hover:bg-white/5 hover:text-lime">{label}</button>)}</div>:null}</header>; }
